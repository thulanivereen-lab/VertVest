import { StyleSheet } from 'react-native';

export const makeStyles = () =>
    StyleSheet.create({
        watchButton: {
        backgroundColor: '#fff',
        borderRadius: 20,
        paddingHorizontal: 20,
        paddingVertical: 10,
        },
        watchText: {
        color: '#000',
        fontWeight: '600',
     },
    });